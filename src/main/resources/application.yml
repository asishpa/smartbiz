spring:
  application:
    name: Smartbiz-App
  datasource:
    url: jdbc:mysql://localhost:3306/smartbiz
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    operations-sorter: method
    
    
    